# Inbox

> Capture only. Anything you want offloaded — todos, ideas, half-formed thoughts. The agent sorts items into `tasks.md` during `ingest tasks`. Reference material lands in `raw/` alongside until processed.

- ...
