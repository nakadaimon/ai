# Alias-heavy

Multiple links with `|alias` form. Aliases should be preserved per link.

See [[target-one|first display name]] for context.

Compare [[target-two|second display]] with [[target-three|third display]].

A plain link too: [[plain-target]] without alias.

And [[target-one|different alias for same target]] — same target, different alias.
