# Combined heading + alias

Links with both `#heading` and `|alias`. All three fields should be present.

See [[target-doc#section-a|the first section]].

Compare [[target-doc#section-b|second part]] with the rest.

Plain: [[plain-target]].

Heading only: [[target-doc#section-c]].

Alias only: [[target-doc|just an alias]].
